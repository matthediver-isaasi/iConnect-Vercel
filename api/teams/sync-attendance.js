// Compatibility route matching the existing Zoom attendance API naming.
// The handler supports both binding (action=bind) and manual resync
// (action=sync, the default).
export { default } from './attendance.js';