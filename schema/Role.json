{
    "name": "Role",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Role name (e.g., 'Standard Member', 'Premium Member', 'Administrator')"
      },
      "description": {
        "type": "string",
        "description": "Description of what this role includes"
      },
      "excluded_features": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [],
        "description": "List of feature IDs that members with this role should NOT have access to"
      },
      "is_default": {
        "type": "boolean",
        "default": false,
        "description": "Whether this is the default role for new members"
      },
      "is_admin": {
        "type": "boolean",
        "default": false,
        "description": "Whether this role has administrator privileges"
      },
      "is_tenant_admin": {
        "type": "boolean",
        "default": false,
        "description": "Whether this is a tenant-level admin role. When true, the role is hidden from the Role selector in the Edit Member modal on /Team."
      },
      "show_tours": {
        "type": "boolean",
        "default": true,
        "description": "Whether users with this role should see page tours"
      },
      "default_landing_page": {
        "type": "string",
        "enum": [
          "Events",
          "Dashboard",
          "Bookings",
          "Balances",
          "BuyProgramTickets",
          "History"
        ],
        "default": "Events",
        "description": "The page users with this role land on after login"
      },
      "layout_theme": {
        "type": "string",
        "enum": [
          "default",
          "new_header"
        ],
        "default": "default",
        "description": "Visual theme/layout to apply for users with this role"
      },
      "requires_effective_from_date": {
        "type": "boolean",
        "default": false,
        "description": "Whether assigning this role requires an Effective From date (e.g., for Alumni)"
      },
      "badge_background_colour": {
        "type": "string",
        "description": "CSS colour (hex) used as the background of this role's name badge. When unset, a neutral default badge style is used."
      },
      "badge_text_colour": {
        "type": "string",
        "description": "CSS colour (hex) used for the text of this role's name badge. When unset, a readable colour is derived from the background or a neutral default is used."
      }
    },
    "required": [
      "name"
    ]
  }