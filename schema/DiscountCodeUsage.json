{
    "name": "DiscountCodeUsage",
    "type": "object",
    "properties": {
      "discount_code_id": {
        "type": "string",
        "description": "Reference to DiscountCode entity"
      },
      "organization_id": {
        "type": "string",
        "description": "Reference to Organization entity"
      },
      "usage_count": {
        "type": "number",
        "default": 0,
        "description": "Number of times this organization has used this discount code"
      },
      "member_id": {
        "type": "string",
        "description": "Optional: Reference to Member entity for per-member usage tracking"
      }
    },
    "required": [
      "discount_code_id"
    ]
  }