{
    "name": "Member",
    "type": "object",
    "properties": {
      "email": {
        "type": "string",
        "format": "email",
        "description": "Member's email address from Zoho CRM"
      },
      "first_name": {
        "type": "string"
      },
      "last_name": {
        "type": "string"
      },
      "handle": {
        "type": "string",
        "description": "Unique public identifier for the member, used in article URLs. Cannot be changed once set.",
        "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$"
      },
      "job_title": {
        "type": "string",
        "description": "Member's job title or position"
      },
      "biography": {
        "type": "string",
        "description": "Member's professional biography (max 500 words)"
      },
      "profile_photo_url": {
        "type": "string",
        "description": "URL to member's profile photo"
      },
      "zoho_contact_id": {
        "type": "string",
        "description": "Zoho CRM Contact ID"
      },
      "organization_id": {
        "type": "string",
        "description": "Reference to Organization entity"
      },
      "role_id": {
        "type": "string",
        "description": "Reference to Role entity - determines base permissions"
      },
      "role_effective_from": {
        "type": "string",
        "format": "date",
        "description": "Effective date for the current role assignment (e.g., when an Alumni member retired)"
      },
      "last_synced": {
        "type": "string",
        "format": "date-time",
        "description": "Last sync from Zoho CRM"
      },
      "last_login": {
        "type": "string",
        "format": "date-time",
        "description": "Last time the member logged in"
      },
      "last_activity": {
        "type": "string",
        "format": "date-time",
        "description": "Last activity/engagement timestamp in the portal"
      },
      "login_enabled": {
        "type": "boolean",
        "default": true,
        "description": "Whether this member is allowed to login to the portal"
      },
      "member_excluded_features": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [],
        "description": "Additional feature IDs excluded for this specific member (in addition to role exclusions)"
      },
      "has_seen_onboarding_tour": {
        "type": "boolean",
        "default": false,
        "description": "Whether the member has completed the onboarding tour"
      },
      "page_tours_seen": {
        "type": "object",
        "additionalProperties": {
          "type": "boolean"
        },
        "default": {},
        "description": "Tracks which page-specific tours the member has seen (e.g., {\"BuyProgramTickets\": true})"
      },
      "show_in_directory": {
        "type": "boolean",
        "default": true,
        "description": "Whether this member should appear in the public member directory"
      }
    },
    "required": [
      "email"
    ]
  }