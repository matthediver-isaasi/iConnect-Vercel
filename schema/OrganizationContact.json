{
    "name": "OrganizationContact",
    "type": "object",
    "properties": {
      "organization_id": {
        "type": "string",
        "description": "Reference to Organization entity"
      },
      "email": {
        "type": "string",
        "format": "email",
        "description": "Contact's email address"
      },
      "first_name": {
        "type": "string",
        "description": "Contact's first name"
      },
      "last_name": {
        "type": "string",
        "description": "Contact's last name"
      },
      "is_active": {
        "type": "boolean",
        "default": true,
        "description": "Whether this contact is still active"
      }
    },
    "required": [
      "organization_id",
      "email"
    ]
  }
