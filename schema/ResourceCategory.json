{
    "name": "ResourceCategory",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Category name (e.g., Webinars, Reports)"
      },
      "description": {
        "type": "string",
        "description": "Optional description of this category"
      },
      "subcategories": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [],
        "description": "List of subcategory names under this category"
      },
      "display_order": {
        "type": "number",
        "default": 0,
        "description": "Order in which this category should appear in filters"
      },
      "is_active": {
        "type": "boolean",
        "default": true,
        "description": "Whether this category is currently active"
      },
      "excluded_role_ids": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [],
        "description": "Member role IDs that cannot see this category (and resources exclusively tagged with its subcategories). Empty array means all roles can see it. A category with any exclusions is member-only (hidden from guests)."
      },
      "subcategory_excluded_role_ids": {
        "type": "object",
        "additionalProperties": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "default": {},
        "description": "Map of subcategory name to member role IDs that cannot see that subcategory within this category. Missing/empty entries follow the category's own visibility. A subcategory with any exclusions is member-only (hidden from guests). Managed via the category-role-access endpoint; renames carry entries over and removals prune them."
      },
      "applies_to_content_types": {
        "type": "array",
        "items": {
          "type": "string",
          "enum": [
            "Articles",
            "Resources"
          ]
        },
        "default": [
          "Articles",
          "Resources"
        ],
        "description": "Content types this category's filters apply to (e.g., Articles only use Theme, Resources use Theme and Level)"
      }
    },
    "required": [
      "name"
    ]
  }