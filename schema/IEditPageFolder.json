{
    "name": "IEditPageFolder",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Folder name"
      },
      "parent_id": {
        "type": "string",
        "description": "Reference to parent IEditPageFolder entity for nested folders (optional)"
      },
      "display_order": {
        "type": "number",
        "default": 0,
        "description": "Order in which this folder should appear within its parent"
      }
    },
    "required": [
      "name"
    ]
  }
