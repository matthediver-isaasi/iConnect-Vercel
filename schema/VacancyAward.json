{
  "name": "VacancyAward",
  "type": "object",
  "properties": {
    "member_group_id": {
      "type": "string",
      "description": "Reference to the MemberGroup this award belongs to"
    },
    "vacancy_id": {
      "type": "string",
      "description": "Reference to the Vacancy whose position is being awarded"
    },
    "awarded_member_id": {
      "type": "string",
      "description": "Reference to the Member who was awarded the position"
    },
    "source_type": {
      "type": "string",
      "enum": [
        "submission",
        "application"
      ],
      "description": "Where the awarded applicant came from: a form 'submission' or a legacy express-interest 'application'"
    },
    "source_id": {
      "type": "string",
      "description": "Id of the originating form_submission or vacancy_application row"
    },
    "awarded_by_member_id": {
      "type": "string",
      "description": "Reference to the Member (group admin) who awarded the position (forced to the current member server-side)"
    }
  },
  "required": [
    "member_group_id",
    "vacancy_id",
    "awarded_member_id"
  ]
}
