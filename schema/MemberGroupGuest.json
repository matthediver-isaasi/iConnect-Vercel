{
    "name": "MemberGroupGuest",
    "type": "object",
    "properties": {
      "first_name": {
        "type": "string",
        "description": "Guest's first name"
      },
      "last_name": {
        "type": "string",
        "description": "Guest's last name"
      },
      "email": {
        "type": "string",
        "format": "email",
        "description": "Guest's email address"
      },
      "organisation": {
        "type": "string",
        "description": "Guest's organisation name (text only, not a managed org)"
      },
      "job_title": {
        "type": "string",
        "description": "Guest's job title"
      },
      "is_active": {
        "type": "boolean",
        "default": true,
        "description": "Whether this guest is active"
      },
      "member_id": {
        "type": "string",
        "format": "uuid",
        "description": "ID of the provisioned member record linked to this guest marker row"
      }
    },
    "required": [
      "first_name",
      "last_name",
      "email"
    ]
  }
