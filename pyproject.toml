[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Local Excel report generation and independent workbook validation"
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1.5",
    "xlsxwriter>=3.2.9",
]
